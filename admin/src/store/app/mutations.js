/*
export function someMutation (state) {
}
*/

export const setAuthToken = (state, token) => state.authToken = token;
export const setUser = (state, user) => state.user = user;
export const setFactTypes = (state, types) => state.factTypes = types;

